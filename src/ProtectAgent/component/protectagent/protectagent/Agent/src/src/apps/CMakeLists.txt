add_subdirectory(app)
add_subdirectory(appprotect)
add_subdirectory(vmwarenative)
if(LINUX_COMPILE)
    add_subdirectory(dws)
    add_subdirectory(informix)
    add_subdirectory(xbsaclientcomm)
endif()