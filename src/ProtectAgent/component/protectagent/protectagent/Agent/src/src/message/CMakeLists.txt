if (LINUX_COMPILE)
    if(PLUGIN_SDK)
        add_subdirectory(tcp)
        add_subdirectory(archivestream)
    else()
        add_subdirectory(curlclient)
        add_subdirectory(rest)
        add_subdirectory(tcp)
        add_subdirectory(archivestream)
    endif()
else()
    add_subdirectory(archivestream)
    add_subdirectory(curlclient)
    add_subdirectory(rest)
    add_subdirectory(tcp)
endif()