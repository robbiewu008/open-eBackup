{{- $deploytype:= include "getDeployType" . | default "" }}

{{- if or (eq $deploytype "a8000") (eq $deploytype "d0")}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sftp-sa
  namespace: dpa
{{- end }}